spring:
  datasource:
    url: jdbc:postgresql://${{ PGUSER }}:${{ PGPASSWORD }}@${{ PGHOST }}:${{ PGPORT }}/${{ PGDATABASE }}
    username: ${PGUSER}
    password: ${PGPASSWORD}
    name: spring-blog
